# Backlog — Overlap

> Pull-based. Finish a component, claim the next. Ordered by the build plan and by
> dependencies. This file is the single source of truth for who is on what.

## How to claim an item

1. Pick the **top unclaimed** item whose dependencies are `🟩 done`.
2. Set its **Status → 🟡 wip** and **Owner → your name** (`frank` / `dylan`), and
   **commit that change first** (`chore(backlog): claim S2.2`) before writing code.
   The commit is what reserves it — if you didn't commit the claim, it's not yours.
3. **WIP limit = 1 per person.** Don't claim a second item until yours is `🟩 done` (merged to `develop`).
4. Don't claim an item another person is already `🟡 wip` on.
5. When merged to `develop`, set **Status → 🟩 done**.

**Legend:** ⬜ todo · 🟡 wip · 🟩 done · ⛔ blocked
**Initial lean** (not a rule, just to reduce context-switching): 0G items → `frank`, Hedera/World items → `dylan`. Anyone can pull anything.

---

## Phase 0 — Foundations & the gamble · Friday night
> The Friday spike is the whole bet. If `attest` can't be verified independently, we know tonight.

| ID | Component | What | Sponsor | Depends on | Owner | Status |
|----|-----------|------|---------|-----------|-------|:------:|
| S0.1 | repo setup | branches, `.env.example`, README skeleton, spec folder | — | — | integrator | 🟩 |
| S0.2 | `spec-03-attest.md` | spec for independent attestation check (before code) | 0G | S0.1 | frank | 🟩 |
| S0.3 | **`spike-attest.ts`** | one sealed 0G call, verify signature **outside** the SDK (`verifyEnvelope`) | 0G | S0.2 | frank | 🟩 **FULL GO** — offline **+ live**, exit 0 |

## Phase 1 — Lock-in · Saturday AM
| ID | Component | What | Sponsor | Depends on | Owner | Status |
|----|-----------|------|---------|-----------|-------|:------:|
| S1.1 | `spec-01-session.md` | spec for room + deadline + commitments | Hedera | S0.1 | | ⬜ |
| S1.2 | `session` | create room, publish expiry to HCS **before** any write, issue link | Hedera | S1.1 | dylan | 🟩 |
| S1.3 | `registry.write` | `sha256(ciphertext)` + timestamp to HCS topic (versioned messages) | Hedera | S1.1 | dylan | 🟩 |
| S1.4 | `seal` (client) | encrypt position in-browser to enclave pubkey (hybrid) | 0G | S0.3 | frank | 🟩 |
| S1.5 | `worldid` | Selfie Check, one nullifier per room per side | World | S0.1 | dylan | 🟩 |

## Phase 2 — Core loop closes · Saturday PM
| ID | Component | What | Sponsor | Depends on | Owner | Status |
|----|-----------|------|---------|-----------|-------|:------:|
| S2.1 | `spec-02-evaluator.md` | spec for sealed evaluation + constrained output (incl. `useCase` prompt hint, D16) | 0G | S0.1 | | ⬜ |
| S2.2 | `evaluator` | 0G call, pinned model, temp 0, **enum output only**; prepends the use-case hint (RF-M6-007) | 0G | S1.4, S2.1 | **frank** | 🟩 **live-verified** (`npm run eval:live` GO) |
| S2.3 | `attest` (module) | verify TEE signature on every verdict, **fail closed**. **Absorbed by S2.9 (26 Jul):** `runReveal` fetches the broker signature, runs `verifyEnvelope` against the pinned key and refuses to build a verdict unless `mayPublish()` — `buildVerdictMessage` *requires* an `attestationRef`, so no type-level path exists from a failed attestation to a published verdict. Every fail-closed branch unit-tested; S3.20 additionally shows `attestation_invalid` loudly in the UI. | 0G | S0.3 | **frank** | 🟩 (via S2.9) |
| S2.4 | `scheduler` | arm + listen for the scheduled reveal | Hedera | S1.2 | dylan | 🟩 |
| S2.5 | `registry.read` | read verdict via Mirror Node REST | Hedera | S1.3 | dylan | 🟩 |
| S2.6 | topic versioning | 3 message types per session (expiry/commitments/verdict), versioned from commit 1 | Hedera | S1.3 | | ⬜ |
| S2.7 | canonical consolidation | migrate `registry` off its own serializer onto `src/lib/canonical.ts`; delete `src/registry/canonical.ts` | — | S1.4 | dylan | 🟩 |
| S2.8 | per-side gap consent → evaluator | **P0.** `c879e20` (per-side `gapOptIn` on the commitment message) + `ea0fc79` (`consentFromCommitments`: derive `consent: { a, b }` from BOTH commitments on the topic, missing/legacy ⇒ `false`, fail-safe) are on `develop`. Consent never comes from the create form (A's prefill only). Remaining: the caller that actually hands that `consent` to `evaluate()` — lands with S2.3, which owns the verdict write path. | 0G/Hedera | S2.2, S1.3 | dylan | 🟡 |
| S2.9 | reveal runner + validaciones | **P0 (para Frank — ver `docs/handoff-frank-validaciones.es.md`).** (a) Wire the reveal: scheduler fires → read both commitments → `evaluate()` (inputs ready on `develop`: `getSealedPayloads`, `consentFromCommitments`) → attest → publish verdict — today nothing writes a verdict. (b) Answer the 4 validations: HTTPS tunnel go/no-go · seal-key yes/no (§3.1) · 4.0-vs-v2 track call · `MEMORIA.md`. **Done (a):** `src/reveal/run-reveal.ts` (orchestrator, 17 tests incl. every fail-closed branch) + `src/evaluator/og-signature.ts` (fetch the broker signature → `Envelope`, 11 tests) + `buildVerdictMessage` + `registry.publishVerdict` + wiring in `verdict/actions.ts`. **Done (b):** all four answered in `docs/handoff-frank-validaciones.es.md`. ⚠️ **Left undone deliberately: `armReveal` is still not called at create.** `hederaScheduleService(buildRevealTx)` needs a transaction for Hedera to run at the deadline, and the only meaningful one is a topic message marking that the clock fired — a FOURTH message type, which `decodeMirrorMessage` would throw on, breaking every read. That belongs with **S2.6**, not a late edit. The reveal fires today on the server-side fallback over the publicly committed deadline (DA5), which is unmovable because the expiry is on the topic before anyone can write. | 0G/Hedera | S2.2, S2.8, S3.2 | **frank** | 🟩 |

## Phase 3 — Usable · Saturday evening
| ID | Component | What | Sponsor | Depends on | Owner | Status |
|----|-----------|------|---------|-----------|-------|:------:|
| S3.1 | `web` create | screen: open a room, set deadline, get QR/link | web | S1.2 | dylan | 🟩 |
| S3.2 | `web` write+seal | screen: write position (preset placeholder + soft checklist, D16), seal in-browser | web | S1.4, S3.5 | dylan | 🟩 |
| S3.3 | `web` verdict | screen: countdown + one-line verdict (Mirror) | web | S2.5 | dylan | 🟩 |
| S3.4 | two-browser E2E | full flow across two browsers, QR to join. **Prereq: HTTPS origin** — phone browsers disable WebCrypto (IDKit bridge + our `seal()`) on plain-HTTP LAN; `APP_URL` must carry the HTTPS origin so QRs encode it (tunnel vs local TLS: pending team OK) | web | S3.1–S3.3 | | ⬜ |
| S3.5 | `usecases` presets | preset module (`src/session/usecases.ts`: labels/placeholder/checklist/evaluatorHint + Zod enum) + create-form use-case picker + `useCase` in the expiry message (D16) | web/Hedera | S3.1 | dylan | 🟩 |
| S3.6 | **`room-qr` demo fixes** | copy button fails **silently** in a non-secure context (`navigator.clipboard` is undefined off HTTPS/localhost), "Copied" never resets, no fallback, no `aria-live`; plus warn when the QR encodes a `localhost` URL — unscannable from a phone. **Found during demo rehearsal.** Touches M8 (dylan's lane) — reassigned with his agreement, he is on World. | web | S3.1 | **frank** | 🟩 |
| **S3.7** | **⛔ position field leaks to the browser** | **P0, BLOCKING S3.2's merge.** `seal-position-form.tsx`'s `<textarea name="position">` sets no `autoComplete`, `spellCheck`, `autoCorrect` or `autoCapitalize`, and the `<form>` sets none either. So the browser may **save the position to disk** (form history, keyed by the field name, offered as a suggestion later — even to the next person on that machine) and, with Chrome's *Enhanced spell check* or Edge's *Microsoft Editor* enabled, **send the text to Google/Microsoft**. Three lines above that field the UI promises *"sealed in this browser — the other side and the operator never see it"*. Same class of hole as D-M6-1 (receiving is already the breach) and earlier in the chain. Fix: `autoComplete="off"` + `spellCheck={false}` + `autoCorrect="off"` + `autoCapitalize="off"` + `data-1p-ignore` + `data-lpignore` on the textarea, `autoComplete="off"` on the form, drop/rename `name="position"`. **Trade-off to decide, not to skip:** `spellCheck={false}` removes the red underline — recommend telling the user why in the UI ("spellcheck is off on purpose: your text never leaves this browser"), which turns a limitation into evidence. | web | S3.2 | **dylan** | 🟡 (fixed as specified + attribute test + in-UI note; pending merge) |
| **S3.8** | **post-create navigation + both links** | After creating a room the QR renders from the create form's React state, so **a plain page reload destroys it** — no URL, no history, nothing to return to. Worse: `session.ts` builds `joinUrls` for **A and B** but `create/actions.ts:22` returns **only B**, so the creator is never shown their own `?side=A` link and cannot get back in to write their own position. Fix: `redirect()` to the existing `/room/[roomId]/share`, and show both links with distinct roles (yours = the way back in; theirs = the one to share). | web | S3.1 | **frank** | 🟩 |
| S3.11 | **rooms section: search + filter** | A dedicated `/rooms` with search and side/label filters; home keeps the 3 most recent + "see all". Needs something recognisable to search BY, so the bookmark gains the **preset label** (`property`/`job`/`otc`) — a closed set the user picked, not free text, so no figures or names can be typed in. **This reverses the S3.9 decision to exclude `useCase`**: consciously, since the user chose it and it is already public on the topic, but a list on a shared laptop now says what KIND of deal — so the UI warns and "forget" stays prominent. The label is passed via a transient `?uc=` on the creator's own redirect, never in the shared join link. | web | S3.9 | **frank** | 🟩 |
| **S3.12** | **reach `/rooms` from the navbar** | **Left undone in S3.11 — the route exists but is nearly unreachable.** Today the only path is the "See all N rooms →" link on the landing page, which renders **only when the list is truncated** (more than 3 rooms). With 1–3 rooms there is no way to reach `/rooms` at all except by typing the URL. Add a link in `site-header.tsx`. Two things to decide rather than assume: **(a)** whether it shows always or only when this device has rooms (an always-on "Rooms" link is a dead end for a first-time visitor, but conditional chrome means the header becomes a client component — it is a server component today, deliberately); **(b)** the word itself is safe ("Rooms" reveals nothing), but keep the S3.10 rule: **no room id, no count, no breadcrumb in the header** — it is the one element in every screenshot. **Decided:** (a) **unconditional** — a link that appears only when this device has rooms makes the header itself disclose that someone here has negotiations open, on every page and in every screenshot, which is the S3.10 rule one step out; it also keeps the header a server component. (b) No count, same reason. Consequence: an empty `/rooms` is now reachable, so `RecentRooms` gained an optional `emptyState` that renders only after the store answers. | web | S3.11 | **frank** | 🟩 |
| S3.10 | **navbar** | No page has a link home — all five are dead ends once you leave the landing. Adds a minimal header in `layout.tsx` (wordmark → `/`) plus the **theme toggle**: `ThemeProvider` is configured with `enableSystem` but no UI exists to change it, so dark mode is currently unreachable by choice. **Note for S3.7/S3.2 (dylan):** a persistent link home makes it one click to leave the write screen and lose an unsealed position, which by design is stored nowhere — worth an unsaved-changes guard on that form. | web | S3.1 | **frank** | 🟩 |
| S3.9 | **recent rooms (bookmarks)** | No accounts, so a closed tab means a lost room unless the URL was saved. Remember rooms **client-side** behind a **swappable port** (`RoomBookmarkStore`, async from day one) so a future server/DB implementation is a new adapter, not a refactor. Stores `{ roomId, side, savedAt }` only — **never anything derived from a position**, never `useCase` (it would reveal the deal *type* on a shared device). Includes "forget" and a TTL. | web | S3.8 | **frank** | 🟩 |
| S3.13 | **worldid 4.0 + Selfie Check** (was claimed as `S3.6`; that id was already frank's room-qr item, claimed 16:57 vs 17:43 — renumbered, content untouched) | migrate M3 to World ID 4.0: IDKit 4.x, server-signed `rp_context` (`WORLD_SIGNING_KEY`), `selfieCheckLegacy` preset, verify via `POST /api/v4/verify/{rp_id}` (plain HTTP — drops the SDK from the server path). Selfie Check is 4.x-only; v2 `device` flow is the fallback if timeboxed out. See `transversal/integration-worldid.md` §5. | World | S3.2 | dylan | 🟡 |
| S3.14 | named demo tunnel | replace the random `trycloudflare.com` quick tunnel with a **named Cloudflare tunnel** on a domain we control: prettier URL for judges, and a persistent connection (quick tunnels ride one anonymous QUIC connection that drops/reconnects — observed live 25 Jul 19:47). Update `scripts/demo-tunnel.sh` + `APP_URL`. Cosmetic + stability; sequence after S3.13/S2.9, before the video if time allows. | web | S3.4 | | ⬜ |
| S3.15 | share/send button | "Send to the ⟨their role⟩…" button on the room-ready screen: **native share sheet** via the Web Share API (`navigator.share({title, text, url})` — the OS's own WhatsApp/Telegram/Mail/AirDrop chooser; needs HTTPS ✓); desktop fallback = small menu (wa.me · t.me/share · mailto: · copy). Prefilled text carries room id + side only — never deal type or terms (same rule as the QR). Mockup: claude.ai artifact `72b7184c` (Dylan-approved). | web | S3.4 | dylan | 🟡 |
| **S3.18** | **⚠️ `verdict-panel` copy claims more than the enum guarantees** | **Found in live E2E, 25 Jul — for dylan (M8 lane).** The verdict rendered **"No deal — several issues block"** (`verdict-panel.tsx:17`). **The pipeline was correct** — checked on the topic: room `r_3d27a10d-fc21-4c77-ae7` carries `gap:multiple` with `[A=true B=true]`, so two-sided consent was real and emitting a `gap:*` was legitimate. The defect is the sentence. **(a) It overstates.** The agreed semantics (`02-data-model.md:82`, `spec-02-evaluator.md`, ledger D9) are: `gap:multiple` = "several dimensions block, **or they are too entangled to attribute to one**". The prompt itself says *"If you cannot cleanly attribute the failure to exactly one dimension, answer gap:multiple"* — so it is also the model's **"I can't tell"** value, and the UI reports that uncertainty as a fact. In a negotiation that is not cosmetic: "several issues block" tells you to walk away, when the truth may be one entangled issue and a phone call. It is the same failure D9 was amended to avoid — a forced pick fabricating an answer — one layer further out. **(b) The colour hierarchy is inverted.** `gap:single` renders amber (a warning) though it is the *hopeful* outcome ("one issue away, worth a call" — `seam-flow-example.md:75`), while `gap:multiple` renders plain foreground, making it **visually identical to `not_workable`** despite carrying information both sides had to consent to. **(c) Nothing explains the restraint** — the reader is not told we deliberately never say *which*, nor that both sides authorised this level of detail, so it reads as evasive rather than protective. **Suggested fix (frank's recommendation, dylan's call):** `gap:single` → "No deal — one issue is in the way"; `gap:multiple` → "No deal — more than one thing is in the way" (true in **both** cases the enum covers, without asserting which), + a subtitle "Both sides agreed to reveal how many. Never which."; swap the tones so `gap:single` is the encouraging one. ⚠️ **Do not fold into S4.9** (copy quality pass): that is a style sweep, this is a correctness bug — the sentence asserts something the enum does not guarantee. Also **out of scope: the colour tokens** (`--workable`/`--not-workable`/`--pending`) belong to **S4.8**; use the existing Tailwind values here to avoid colliding with the rebrand. | web | S3.3 | **frank** (reassigned by the integrator, 26 Jul — dylan not on it) | 🟡 |
| S3.16 | role declaration + context anchor | retroactive row (built during live testing, in PR #18 — logged per the every-item-gets-a-row rule): create form asks "You are the…" (labels follow the preset) + optional "What's this about?" anchor (public-class, on the expiry message, shown on the write screen); role-framed room-ready screen. Fixes both humans entering side B. Dylan's design. | web | S3.5 | dylan | 🟡 (in PR #18) |
| S3.17 | share-screen durable context | retroactive row (in PR #18): bare `/share` links (verdict's "Back to QR") lost role/context — now deal type + about read from the topic via Mirror, viewer side resolves declared → device bookmark (S3.9) → default; bookmark never overwritten by a resolved default. | web | S3.16, S3.9 | dylan | 🟡 (in PR #18) |
| S4.9 | copy quality pass | UI/UX best practices across user-facing text: plain language, no operator overclaims (landing "or us" dropped; invite amended ✓), moderate the em-dash habit, consistent role naming. Rides with S4.8's rename touchpoints. | web/compliance | S4.8 | dylan | 🟡 |
| **S3.19** | **verdict loading state (spinner)** | While a room waits for its verdict the panel shows a static "Sealed / Awaiting the reveal" and nothing moves, so a judge watching the demo cannot tell the difference between *working* and *hung* — and the reveal really does take seconds (enclave call + attestation + topic write + Mirror indexing). Add a spinner. **The design point: there are TWO waits, not one.** Before the deadline nothing is happening — a spinner there would claim work that is not occurring, and would spin for hours. After the deadline the lazy reveal is genuinely running (`revealRoom()` fires on the first reader, S2.9). Only the second gets a spinner; the first keeps the countdown. Accessible: `role="status"` + `aria-live` already on the panel, spinner `aria-hidden` (the text carries the meaning), and it must honour `prefers-reduced-motion` — there is no global rule for that in `globals.css` yet. **Done:** new `spinner` component (`aria-hidden`, inherits `currentColor`, `motion-reduce:animate-none`) + `verdict-panel` gains `deadlineReached` + `verdict-view` derives it client-side with the same mount-safe pattern as `countdown` (no hydration mismatch). The revealing copy also states the fail-closed rule at the one moment it is being exercised. 11 RTL tests, 2 stories; verified in a real browser on a seeded past-deadline room. ⚠️ **Known gap, see S3.20:** a room that can never resolve (one side never committed) spins forever. | web | S3.3, S2.9 | **frank** | 🟩 |
| S3.20 | verdict spinner has no end state | Fallout of S3.19, worth its own row. `readVerdictAction` returns `null` for every non-verdict outcome, so a room the reveal can **never** finish — one side never committed, the ciphertext was lost to a restart (`missing_sealed_payload`), the attestation failed — now spins forever. Before S3.19 it read "Sealed" forever, which was equally wrong but looked less like an imminent answer; a spinner promises something is about to happen. `revealStatusAction` already returns a typed `{ blocked: reason }` and nothing consumes it. Fix: poll that instead and render a terminal state per reason — and for `attestation_invalid` say so loudly, because "no valid attestation ⇒ no verdict" is a guarantee we WANT a judge to see working rather than mistake for a hang. **Done (26 Jul):** `revealStatusAction` now returns the **typed** reason (`RevealFailure`) + `publishedAt`, subsumes `readVerdictAction` (deleted), and the screen polls it. Reasons render in three shapes: **terminal** (`attestation_invalid` — loud, red, states the fail-closed rule as the outcome it is · `missing_sealed_payload` · `unseal_failed` · `no_expiry`) stop the poll, kill the spinner AND the dead countdown; **waiting** (`incomplete_commitments`) shows "Waiting for the other side" with no spinner (nothing is running) but keeps polling in case they commit late; **transient** (`evaluation_failed`/`attestation_unavailable`/`publish_failed`/`already_published`) keep the spinner and name what failed. 9 new tests (view + panel), 4 stories. | web | S3.19 | **frank** | 🟩 |
| **S3.21** | **⛔ P0 · reveals concurrentes: gasto real duplicado y el topic ensuciado** | **Evidencia en el topic (26 jul, 00:45): 3 salas con veredictos duplicados, una con TRES** — `r_d05066a4…` a las 23:59:02.8 / 23:59:06.0 / 23:59:10.4 · `r_b5f5b0f8…` 23:45:07.2 / 23:45:14.9 · `r_3d27a10d…` 22:28:04.1 / 22:28:04.9. Los tres coincidieron en el valor, así que nadie lo notó; no estaba garantizado. **Dos mecanismos, ambos reales, y hay que arreglar los dos o el bug sigue.** **(a) Los sondeos se solapan.** `verdict-view.tsx` usa `setInterval(async () => …, pollMs)` y `setInterval` **no espera** a la llamada anterior; cada sondeo dispara `revealRoom()`, que tarda mucho más de 5 s (leer topic → des-sellar → **llamada al enclave** → pedir firma → verificar → escribir). Fix: sustituir por un `setTimeout` que se re-programa **al terminar** cada llamada (y limpiarlo en el cleanup del efecto). **(b) No hay cerrojo en el servidor.** Las dos partes suelen tener la pantalla de veredicto abierta a la vez; cada navegador sondea por su cuenta y ninguno ve el reveal del otro. El guardián `already_published` de `run-reveal.ts` lee el topic vía Mirror, que **tarda ~3 s en indexar**, así que no sirve para nada más rápido que eso. Fix: `Map<roomId, Promise<RevealResult>>` a nivel de módulo en `src/reveal/reveal-service.ts` — si ya hay un reveal en vuelo para esa sala, **devolver esa misma promesa** en vez de lanzar otro; borrar la entrada en el `finally`. **Coste de no arreglarlo:** cada duplicado es una llamada al enclave que pagamos (~0.0005 0G), y el topic —del que decimos que es la fuente de verdad pública— acaba con N veredictos para una sala que debe tener uno. **⚠️ Lo que esto NO cierra:** con varias instancias en Vercel el cerrojo es por proceso y dos instancias pueden duplicar igual. Cerrarlo de verdad exige que el topic arbitre, y los 3 s de Mirror lo impiden. Documentarlo como limitación conocida, no fingir que está resuelto. **Hecho (26 jul):** (a) `verdict-view` re-programa con `setTimeout` **al terminar** cada sondeo (y sobrevive a un fallo transitorio de red en vez de quedarse colgado); (b) `src/reveal/in-flight.ts` — `createInFlight`, dedupe por sala sobre **la misma promesa**, entrada borrada en `finally` para que un reveal fallido no bloquee la sala; la limitación multi-instancia queda documentada en el propio módulo. 8 tests nuevos (6 unit + 2 RTL). | web/0G | S2.9 | **frank** | 🟩 |
| **S3.22** | **la pantalla de veredicto se queda en «Reveal due · now» para siempre** | La cuenta atrás se sigue renderizando **después** del reveal, y como el plazo ya pasó queda clavada en `now`. Capturado en vivo en una sala ya resuelta: `2 OF 2 SIDES COMMITTED · REVEAL DUE now · Not workable — no deal`. **La causa es concreta y el dato ya lo tenemos:** el mensaje de veredicto lleva `publishedAt` (la marca de consenso de cuándo se publicó) y `src/app/room/[roomId]/verdict/page.tsx` **lo tira** — se queda sólo con `view.verdict?.verdict`. Fix: pasar también `publishedAt` a `VerdictView`, y cuando hay veredicto sustituir el `<Countdown>` por *"Revealed 26 Jul, 00:14"* en lugar de una cuenta atrás vencida. **Por qué merece la pena más allá de lo cosmético:** una hora de publicación verificable contra el topic es justo el tipo de dato que sostiene el discurso ante un jurado; ahora mismo lo estamos descartando. **Hecho (26 jul):** `readVerdictAction` devuelve `{ verdict, publishedAt }` (también en el camino del reveal), la página conserva `publishedAt` y `VerdictView` sustituye el countdown por «Revealed 26 Jul 2026, 00:14 UTC» — en **UTC deliberadamente**: determinista entre server y cliente (sin mismatch de hidratación) y contrastable con los timestamps del topic. 5 tests RTL + story `Resolved`. | web | S3.3 | **frank** | 🟩 |
| **S3.23** | **la pantalla de escritura ofrece formulario a un lado que ya comprometió** | `src/app/room/[roomId]/write/page.tsx` lee del topic **sólo el `expiry`** (`roomFound = Boolean(view.expiry)`); nunca mira `view.commitments`. Así que quien vuelve al enlace ve un formulario vacío, **reescribe toda su posición**, pasa el Selfie Check y sella — y sólo entonces el servidor lo rechaza con `seat r_x:A is already taken`, un mensaje que no significa nada para una persona. Y probablemente muera antes: la acción de World se auto-crea con `max_verifications: 1`, así que el segundo intento falla en el gate con un error de World. **El dato está a salvo** — `claimSeat` lanza en `submitCommitmentAction` **antes** de la línea que guarda el ciphertext, y una comprobación del topic (26 jul) da **0 lados duplicados en 8 salas**. Lo roto es la experiencia: se pierde todo el trabajo en el peor momento. Fix: leer los commitments en la página y, si ya existe uno para ese lado, mostrar "ya enviaste tu posición" + enlace al veredicto en vez del formulario. **Hecho (26 jul):** la página lee `view.commitments` y, si el lado del enlace ya tiene commitment en el topic, renderiza «You already sent your position» + botón al veredicto en lugar del formulario; el copy dice por qué (el commitment es vinculante). El aviso de "no deadline" mantiene prioridad (sin expiry legible, la verdad es Mirror lag). 4 tests RTL de la página (primer test de página del repo, con registry/env mockeados). | web | S3.2 | **frank** | 🟩 |
| **S3.24** | **endurecer contra commitments duplicados (el «respaldo duradero» no existe)** | `write/actions.ts:38-40` afirma en un comentario: *"the topic's one-commitment-per-side rule is the durable backstop"*. **Esa regla no está implementada en ninguna parte** — `publishCommitment` valida el esquema y escribe; nada consulta el topic. El único guardián es `seats`, un `Map` **en memoria del proceso**, así que un reinicio libera el asiento y en Vercel cada instancia tiene el suyo. **Qué rompe un duplicado si llega a colarse:** (i) `consentFromCommitments` exige que **todos** los commitments de un lado digan `true`, así que un reenvío con la casilla desmarcada **revoca en silencio** un consentimiento que la otra parte ya había igualado; (ii) `sealedByRoom` está indexado por `(sala, lado)` y **sobrescribe**, así que el enclave juzgaría el segundo texto mientras el topic lleva los dos hashes — y *"no puedes cambiar tu posición después de comprometerla"* deja de ser cierto. **Tres arreglos, de más barato a más caro:** **(a)** 2 líneas — `sealedByRoom` **no sobrescribe**: si ya hay payload para ese lado se conserva el primero, de modo que el ciphertext siempre corresponda al commitment vinculante; **(b)** `runReveal` usa el commitment **más antiguo** por lado (menor sequence number) en vez de tolerar duplicados, para que el vínculo sea al primero y el consentimiento no se pueda revocar a posteriori; **(c)** rechazar duplicados contra el topic en `submitCommitmentAction` antes de publicar. **⚠️ Sobre (c):** Mirror tarda ~3 s en indexar, así que cierra el caso del reinicio pero **no** la carrera rápida — no lo vendas como resuelto. (a) es gratis y merece hacerse siempre. **Hecho (26 jul), las tres partes:** **(a)** el vault no sobrescribe (`sealedByRoom.has()` antes de `set`); **(b)** `runReveal` vincula al commitment **más antiguo** por lado — ni el hash juzgado ni el consentimiento pueden cambiar con un duplicado (en ninguna dirección: tampoco *conceder* consentimiento a posteriori), y dos commitments del mismo lado no completan una sala; **(c)** `submitCommitmentAction` consulta el topic antes de todo efecto y rechaza el duplicado con mensaje humano — **antes del gate de World**, para no quemar la única verificación de la persona en un submit condenado; si Mirror está caído no bloquea (el seat claim sigue de guardia). El comentario mentiroso del "durable backstop" ahora dice la verdad con su caveat (~3 s de Mirror ⇒ cierra el reinicio, no la carrera rápida). 7 tests nuevos. | web/Hedera | S3.2, S2.9 | **frank** | 🟩 |
| S2.10 | lazy-reveal explainer (docs) | **frank:** the "first reader past the deadline runs the reveal" model confused a teammate on first contact — write it in plain words for non-readers-of-code: one paragraph + one analogy in `M5`/`M6` docs and ONE line for the demo narration (e.g. "there is no alarm clock — the first person to look at the clock after the deadline is the one who turns the lights on; everyone after finds them already on"). Include why it's safe (public deadline, first-writer-wins, N−1 get `already_published`). **Done (26 Jul):** `M5-scheduler.md` §7 "The lazy reveal, in plain words" (the full paragraph + the three safety facts + the narration line) and a cross-reference paragraph in `M6-evaluator.md` §6 answering "who actually calls this". | docs | S2.9 | frank | 🟩 |

## Phase 4 — Demo & track requirements · Saturday late
| ID | Component | What | Sponsor | Depends on | Owner | Status |
|----|-----------|------|---------|-----------|-------|:------:|
| S4.1 | `inspect.ts` | demo: show our store holds only ciphertext (no key) | demo | S1.3 | dylan | 🟩 |
| S4.2 | `demo-naive.ts` | demo: same product w/o enclave → plaintext leaks | demo | S2.2 | **frank** | 🟩 (`--live` shows the SAME verdict) |
| S4.3 | World testing doc | developer friction + user friction (track requirement) | World | S1.5 | dylan | 🟡 (dev half + skeleton; user half after S3.2) |
| S4.4 | README (final) | what/why, architecture, how to run, sponsors. **Done** — written as **Overlap** (DA11). Leads with the problem, then hands the reader four commands to check our claims without trusting us (`spike` / `reveal:live` / `inspect` / `demo:naive`) plus the on-chain curl for the enclave key, and names a live verdict already on the topic (`r_live_ms0v9ctq`). Carries an explicit **"what we prove and what we don't"**: last-link-only attestation (no TDX quote parsing), server-side unsealing (D-M6-2), the in-memory ciphertext window between commits, and why the pinned model is deliberately the weaker one. ⚠️ The **code** rename is still S4.8 (dylan) — this is the README only. | compliance | — | **frank** | 🟩 |
| S4.5 | `ai-usage.md` | which files were AI-assisted (keep updated all weekend) | compliance | — | | ⬜ |
| S4.6 | output vocabulary | enum verdicts + opt-in `gap:*` consent logic | 0G/web | S2.2 | | ⬜ |
| S4.7 | repo hygiene sweep | pre-freeze: decide `MEMORIA.md` (pre-pivot, ES — D2; owner's call) · confirm no editor-history/temp paths tracked (`.history/` incident, fixed) · drop stale local drafts or fold them in (`README2.md` → S4.4) · final `.gitignore` re-check | compliance | — | **frank** | 🟡 |
| S4.8 | brand — apply Overlap | per DA11 (confirm w/ Frank first): **tokens** → `globals.css` (integrator) from design-system §Brand — Overlap (indigo `--primary` + `--side-a`/`--side-b`/`--seam` + semantic `--workable`/`--not-workable`/`--pending`); **`<OverlapMark>`** logo component (chevron SVG) + `overlap` wordmark; migrate `verdict-panel` off placeholder Tailwind colours onto the tokens; **rename** Seam→Overlap (README/docs, UI copy, `roomActionId` prefix `seam-`→`overlap-`, World app display name). **Tagline (Dylan, 25 Jul ~23h):** headline "Stop negotiating deals that were never possible" + kicker "One line. No leaks." | web/compliance | DA11 | dylan | 🟡 |

## Phase 5 — Submit · Saturday 22:00 → Sunday 07:00
| ID | Component | What | Sponsor | Depends on | Owner | Status |
|----|-----------|------|---------|-----------|-------|:------:|
| S5.1 | video | record 2:30, 720p+, no AI voiceover, feature freeze first | compliance | all | | ⬜ |
| S5.2 | submit | re-read sponsor pages, submit ~2h early | integrator | S5.1 | | ⬜ |

---

## Critical path
`S0.2 → S0.3 (spike)` gates everything on 0G. In parallel, Hedera side can start `S1.1 → S1.2/S1.3`
without waiting. The core loop (`S2.2` + `S2.3` + `S2.5`) is the "it works" milestone; the web UI and
the two demo scripts (`S4.1`, `S4.2`) are what actually win the room.

**⛔ `S2.8` is P0 and blocks the next merge to `develop`.** It is the difference between a consent
guarantee we can demonstrate and one we only describe. A judge asking *"where does the other party
agree to this?"* is a question we should want, not fear — and right now the honest answer is that they
don't, because one person ticks a box for both. `c879e20` on `develop-dylan` already does the schema
half; what is missing is reading both commitments and passing `{ a, b }` into `evaluate()`.

---

## Definition of Done (every item)

An item is `🟩 done` only when ALL of these hold. **This is the merge gate** — same discipline as our
previous project (test + story + document, every time).

**Tests** (see `docs/transversal/quality-and-testing.md`)
- Non-trivial component → implementation + **RTL test (Vitest)**, co-located.
- Module logic (seal/commitment **determinism**, registry parsing, `attest` verify) → **unit tests**.
- Critical flow → **Playwright E2E** (the two-browser session).
- Mock 0G/Hedera/World in unit tests; use real services in E2E where feasible.

**Stories**
- Non-trivial UI component → a **Storybook story** (variants/states), co-located.

**Docs — document after each item (do NOT skip this)**
- Update the item's **module doc** (`docs/modules/Mx-*.md`): tick its RF/RNF, record any decision made.
- Update **`CLAUDE.md`** status if the item changes how the system works.
- Log AI-assisted files in **`docs/ai-usage.md`**.
- The spec (`docs/spec-*.md`) was committed **before** the code (spec-driven rule).

**Green + safe**
- `typecheck` + `lint` + `test` + `build` all pass.
- Guardrails respected: no free text from the enclave, **fail closed**, no user private keys, no secrets committed.

Then: set Status → 🟩 `done`, open a PR to `develop` (**no squash**), the integrator reviews and merges.
A component isn't "done" because it runs — it's done when it's **tested, story'd, documented, and green**.
