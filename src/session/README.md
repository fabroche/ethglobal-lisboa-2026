# `session` — room + deadline + link

Backlog **S1.2** · lean owner: `dylan` (Hedera) · spec: `docs/spec-01-session.md`, `docs/modules/M1-session.md`

Create a room, publish its **expiry to Hedera HCS before anyone writes**, and issue the join link/QR.
The deadline being public first is what stops the opener from using it as leverage.
