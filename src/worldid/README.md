# `worldid` — one seat per side

Backlog **S1.5** · lean owner: `dylan` (World) · spec: `docs/modules/M3-worldid.md`

World **Selfie Check** as an abuse signal (not login): issue **one nullifier per room per side**.
Without it, twenty probing submissions reconstruct the other side's number. Scope the action per room
at runtime. Remember the **World testing doc** (developer + user friction) is a track requirement.
